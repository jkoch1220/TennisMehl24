# Netlify Konfiguration für TennisMehl24

[build]
  command = "npm run build"
  publish = "dist"

# Secrets Scanning: Deaktiviere für nicht-sensible öffentliche Werte
# PROJECT_ID, PROJECT_NAME und ENDPOINT sind öffentlich und nicht geheim
# Diese müssen als Umgebungsvariablen im Netlify-Dashboard gesetzt werden
# SECRETS_SCAN_OMIT_KEYS wird als Umgebungsvariable im Netlify-Dashboard gesetzt

