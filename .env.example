# ============================================================================
# TennisMehl24 - Umgebungsvariablen
# ============================================================================
#
# SICHERHEITSHINWEIS:
# - VITE_ Variablen werden im Browser exponiert (nur für öffentliche Werte!)
# - Server-seitige Variablen (ohne VITE_) bleiben geheim
#
# Kopiere diese Datei nach .env.local und fülle die Werte aus.
# ============================================================================


# ============================================================================
# CLIENT-SEITIGE VARIABLEN (im Browser sichtbar - NUR öffentliche Werte!)
# ============================================================================

# Appwrite Basis-Konfiguration (öffentlich)
VITE_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
VITE_APPWRITE_PROJECT_ID=your-project-id

# Backend URL (optional, default: /.netlify/functions)
# VITE_BACKEND_URL=/.netlify/functions

# Diesel Durchschnittspreis Fallback (optional)
# VITE_DIESEL_DURCHSCHNITTSPREIS=1.55


# ============================================================================
# SERVER-SEITIGE VARIABLEN (NUR auf Server/Netlify - GEHEIM!)
# ============================================================================
# Diese Variablen werden in Netlify unter Site Settings > Environment Variables
# konfiguriert oder in .env.local für lokale Entwicklung.

# Appwrite Admin API Key (NIEMALS VITE_ prefix!)
APPWRITE_API_KEY=your-appwrite-api-key

# Anthropic Claude AI (NIEMALS VITE_ prefix!)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Google Maps APIs (NIEMALS VITE_ prefix!)
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# OpenRouteService (NIEMALS VITE_ prefix!)
OPENROUTESERVICE_API_KEY=your-openrouteservice-api-key

# TankerKoenig Diesel-Preise (NIEMALS VITE_ prefix!)
TANKERKOENIG_API_KEY=your-tankerkoenig-api-key


# ============================================================================
# LOKALE ENTWICKLUNG
# ============================================================================
# Für das Setup-Script (scripts/setup-database.ts) werden auch diese benötigt:

# APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
# APPWRITE_PROJECT_ID=your-project-id
# APPWRITE_DATABASE_ID=main


# ============================================================================
# ENTFERNTE VARIABLEN (NICHT MEHR VERWENDEN!)
# ============================================================================
# Die folgenden Variablen wurden aus Sicherheitsgründen entfernt:
#
# ❌ VITE_ANTHROPIC_API_KEY     -> Jetzt: ANTHROPIC_API_KEY (Server-only)
# ❌ VITE_APPWRITE_API_KEY      -> Jetzt: APPWRITE_API_KEY (Server-only)
# ❌ VITE_GOOGLE_MAPS_API_KEY   -> Jetzt: GOOGLE_MAPS_API_KEY (Server-only)
# ❌ VITE_OPENROUTESERVICE_API_KEY -> Jetzt: OPENROUTESERVICE_API_KEY (Server-only)
# ❌ VITE_TANKERKOENIG_API_KEY  -> Jetzt: TANKERKOENIG_API_KEY (Server-only)
